@startuml 中点圆算法流程图
start
:输入圆心(xc, yc)和半径r;
:初始化x = 0, y = r;
:初始化判别式d = 1 - r;
repeat
  :绘制8个对称点;
  if (d < 0?) then (是)
    :d += 2*x + 3;
  else (否)
    :d += 2*(x - y) + 5;
    :y--;
  endif
  :x++;
repeat while (x <= y?) is (是)
stop
@enduml
