@startuml 边界填充算法流程图
start
:输入种子点(x, y)、填充色、边界色;
:创建种子栈;
:将种子点入栈;
repeat
  :从栈中取出一个点;
  if (点在边界内?) then (否)
    :continue;
  endif
  :找到当前行的左右边界;
  :填充当前行;
  :扫描上下相邻行;
  if (发现新的种子点?) then (是)
    :将新种子点入栈;
  endif
repeat while (栈不为空?) is (是)
stop
@enduml
